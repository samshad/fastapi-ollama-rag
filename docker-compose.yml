services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fastapi-ollama-rag-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    # This ensures your container can reach Ollama running natively on your host OS
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/docs"]
      interval: 30s
      timeout: 10s
      retries: 3
